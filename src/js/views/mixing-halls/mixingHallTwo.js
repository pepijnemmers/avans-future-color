import { renderMixingHallPanel } from "./mixingHall.js";

/**
 * @returns {template} The template with mixing hall 2
 */
export function renderMixingHallTwoPanel() {
    return renderMixingHallPanel(2);
}
